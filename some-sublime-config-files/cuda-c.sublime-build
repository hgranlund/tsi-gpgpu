{
	"cmd": ["make"],
	"file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
	"working_dir": "${file_path}",
	"selector": "source.cu, source.c",

	"variants":
	[
		{
			"name": "Run",
			"cmd": ["make" ,"run"],
		},
		{
			"name": "nvcc",
			"cmd": ["nvcc", "${file}", "-o", "${file_path}/${file_base_name}"],
		},
		{
			"name": "nvcc and Run",
			"cmd": ["bash", "-c", "nvcc '${file}' -o '${file_path}/${file_base_name}' && '${file_path}/${file_base_name}'"]
		}

	]
}

